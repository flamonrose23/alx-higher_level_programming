#!/usr/bin/python3
if __name__ == "__main__":
    import sys

    argc = len(sys.argv) - 1
    x = 0
    total = 0
    for arg in sys.argv:
        if c != 0:
            total = total + int(arg)
        else:
            x = x + 1
        print("{:d}".format(result))
